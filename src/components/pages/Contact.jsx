import { instrumentSans } from "@/app/fonts";

export default function Contact() {
	return (
		<div
			className={`w-full h-full ${instrumentSans.className} text-xl font-medium`}
		>
			c
		</div>
	);
}
